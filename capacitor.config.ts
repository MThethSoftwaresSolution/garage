import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'za.co.garage',
  appName: 'garage',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }

};

export default config;
